/*
//Write a closure function 
function test() {
    // Variable declation and assignment in the first function block
    // when we are in the function scope we say it as local scope
    const a =10;
    const b =20;
    const c =30;
    function summation(){
       console.log("inner level three",a,b,c);
       return  a + b + c;
   }
   return summation;
 }
 const result = test();
 console.log('checking result', result());
 */

 /*function test(d) {
    // Variable declation and assignment in the first function block
    // when we are in the function scope we say it as local scope
    const a =10;
    const b =20;
    const c =30;
    //function summation(b=30,c=40){
       console.log("inner level three",a,b,c);
       return  a + b + d ;
   }
   //return summation;
// }
 //const result =
    test(50);
 //console.log('checking result', result());
 *//*
 function test(d) {
    // Variable declation and assignment in the first function block
    // when we are in the function scope we say it as local scope
    const a =10;
    const b =20;
    const c =30;
    function summation(b=30,c=40){
       console.log("inner level three",a,b,c);
       return  a * b / d ;
   }
   return summation;
}
 const result =
    test(50);
 console.log('checking result', result());*/


/*function test() {
    // Variable declation and assignment in the first function block
    // when we are in the function scope we say it as local scope
    const a =10;
    const b =20;
    const c =30;
    function summation(b=30,c=40){
       console.log("inner level three",a,b,c);
       return  a + b + c;
   }
   return summation;
 }
 const result = test();
 console.log('checking result', result());
 */
/*function test() {
    // Variable declation and assignment in the first function block
    // when we are in the function scope we say it as local scope
    const a =10;
    const b =20;
    const c =30;
    function summation(b=30,c=40){
       console.log("inner level three",a,b,c);
       return  a + b + c;
   }
   return summation;
 }
 const result = test(50);
 console.log('checking result', result());
 */
/*
function test() {
    // Variable declation and assignment in the first function block
    // when we are in the function scope we say it as local scope
    const a =10;
    const b =20;
    const c =30;
    function summation(b=30,c=40){
       console.log("inner level three",a,b,c);
       return  a + b ;
   }
   return summation;
 }
 const result = test();
 console.log('checking result', result());
 */