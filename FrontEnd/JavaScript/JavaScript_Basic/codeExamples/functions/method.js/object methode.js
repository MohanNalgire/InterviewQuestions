const user = { name: 'mahadev', age: 24};
console.log(Object.keys(user))    // Get object keys =[name,age]

console.log(Object.values(user));    // Get object values =[mahadev 24]

console.log(Object.entries(user));   // Get key-value pairs as arrays [name mahadev, age 24]