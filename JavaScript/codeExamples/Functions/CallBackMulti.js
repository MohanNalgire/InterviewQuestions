function myMul(para){
    console.log(para);

}
//first time we can call the mul function and stored into the result variable
//then reult pass to the muMulfunction and catched buy the 'para' parameter.
function mul(num1,num2){
    let multi=num1*num2;
    return multi;

}

let result=mul(4,6);
myMul(result);

//D:\projects\InterviewQuestions\JavaScript\codeExamples\Functions\CallBackMulti.js